---
  - name: Copy and unarchive backend
    unarchive:
      src: ~/project/backend.tar.gz
      dest: ~/

  - name: Start Server
    shell:
      chdir: ~/backend/dist
      cmd: pm2 start npm -- run start